import  styled  from "styled-components";

export const NavbarContainer = styled.header `
    margin-top: 0;
    width: 100%;
    display: flex;
    background:var(--hdrBnd);
    justify-content: space-between;
    align-items: center;
    max-height: 65px;
    padding: 0rem 0.5rem 0rem 1rem;
    position: sticky;
    top: 0;
    z-index: 1
`